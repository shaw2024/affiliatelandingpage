import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "Affiliate Blog - Curated Product Recommendations",
  description: "Discover the best books, gadgets, and products curated from Facebook stories with Amazon affiliate links",
  keywords: "affiliate marketing, amazon associates, product reviews, book recommendations, gadgets",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
